{
  "component_id": 3,
  "name": "State Machine / Session Manager",
  "version": "1.0",
  "owns": "Task phase, allowed transitions, session memory of what is required vs completed",
  "inputs": ["task_initiation", "hook_outcomes", "tool_outcomes", "escalation_signals"],
  "outputs": ["current_phase", "pending_obligations", "terminal_state_decision"],
  "must_never": [
    "Permit unapproved phase skipping"
  ],
  "depends_on": [4, 7, 8, 18],
  "failure_modes": ["state_corruption", "partial_completion", "skipped_validation"],
  "code_modules": [
    "ironframe/state/session_v1_0.py",
    "ironframe/state/phase_v1_0.py"
  ],
  "rtm_requirements": ["IF-REQ-003", "IF-REQ-009"],
  "status": "implemented",
  "notes": "Session coordinator reads 3 backing stores without merging. Phase gate supports YAML frontmatter declarations with backward compat."
}
